version: 2

models:
    - name: d_survey
      description: "Count the number of rows"
      columns:
          - name: survey_id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
                -row_count:
                  equal: 50

    - name: d_item
      description: " "
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: 1.max_quantity_purchased
      description: "max quality purchased"
      columns:
          - name: Username
            description: "The primary key for this table"
            tests:
                - unique
                - not_null